<!DOCTYPE HTML>
<html>
<head>
    <title>ZaPishka(ADMIN)</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8"/>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <link rel="shortcut icon" href="https://image.flaticon.com/icons/png/512/93/93155.png">
</head>
<body>
<div>Редакт юзера:</div>
<form action="/user" method="post">
    <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
    <input type="text" value="{{user.username}}" name="username">
    <div>User roles now: {{user.roles}}</div>
    {{#role}}
        <div>
            <label><input type="checkbox" name="{{name}}">{{name}}</label>
        </div>
    {{/role}}
    <input type="hidden" value="{{user.id}}" name="userId">
    <button type="submit">Save</button>
</form>
</body>
</html>